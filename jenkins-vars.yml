---

# jenkins variables
project_name: docker-ppsspp
external_type: na
custom_version_command: |-
  curl -sL https://archlinux.org/packages/extra/x86_64/ppsspp/ |awk -F'(<h2>ppsspp |</h2>)' '/<h2>/ {print $2}'
release_type: stable
release_tag: latest
ls_branch: master
build_armhf: false
repo_vars:
  - BUILD_VERSION_ARG = 'PPSSPP_VERSION'
  - LS_USER = 'linuxserver'
  - LS_REPO = 'docker-ppsspp'
  - CONTAINER_NAME = 'ppsspp'
  - DOCKERHUB_IMAGE = 'linuxserver/ppsspp'
  - DEV_DOCKERHUB_IMAGE = 'lsiodev/ppsspp'
  - PR_DOCKERHUB_IMAGE = 'lspipepr/ppsspp'
  - DIST_IMAGE = 'arch'
  - MULTIARCH = 'false'
  - CI = 'true'
  - CI_WEB = 'true'
  - CI_PORT = '3001'
  - CI_SSL = 'true'
  - CI_DELAY = '120'
  - CI_DOCKERENV = 'TZ=US/Pacific'
  - CI_AUTH = 'user:password'
  - CI_WEBPATH = ''
